# My Contributions · Case Studies

我負責**前端 UI/視覺化**與 **Godot 2D 虛擬大廳**。以下「情境 → 做法 → 結果」。
（跟後端的 API/WebSocket 串接、Godot 的 `Backend.gd` 不是我負責。）

---

## Case 1 — CCND 概念網路圖（D3.js）
> 🖼️ `assets/ccnd-graph.png`

- **情境**：要即時呈現「一個人在對話中的推理脈絡」；後端給一棵語意樹資料，但資料不會自己變成圖。
- **做法**：用 **D3.js 力導向圖**畫成可縮放、有層次的網路圖；處理 root/錨點/觀點的視覺權重、縮放互動，配合時間軸回放畫某時點的樹快照。
- **結果**：把最抽象的推理結構變成研究者/使用者都看得懂的視覺，是本專案視覺化的核心。學到 D3 資料綁定、力導向布局，以及「呈現層只吃整理好的資料」的分工。

## Case 2 — Godot 2D 虛擬大廳架構
> 🖼️ `assets/godot-lobby.png` · `assets/godot-issue.png`

- **情境**：2D 多人世界——相遇、張貼議題、表情回應、聊天、語音。
- **做法**：核心決定是 **RPC 掛玩家節點、UI 只做本地呈現**（節點在每端路徑一致才能用 `rpc_id` 精準送達，UI 不含 RPC）；議題廣播 + 頭頂泡泡即時更新；「每位讀者一個表情、可覆蓋」；晚加入者連上即同步既有議題/表情；語音本地 DSP（變聲/殘響/頻譜）與網路解耦。
- **結果**：第一次做即時多人 + 音訊，學到「狀態同步靠節點路徑一致」與網路/呈現分層。

## Case 3 — Godot 網頁顯示 Debug
> 🖼️ `assets/godot-letterbox-before-after.png`（可選）

- **情境**：Godot 匯出成網頁嵌入後，兩側大片黑邊。
- **做法**：先定位是引擎 letterbox（非前端 CSS）→ `stretch/aspect` 由 `keep` 改 `expand` 填滿 → 補 `default_clear_color` 處理露出的地圖外區域 → 最後發現「改了沒變」是**瀏覽器快取 + dev server 沒重開**。
- **結果**：一次系統性除錯練習——先定位問題在哪一層再逐一驗證，並記住「產物被快取」這種非程式的坑。
