# 我負責的部分

我在 TakeABridge 負責前端畫面與 Godot 2D 虛擬大廳。以下用「情境 / 做法 / 結果」記錄。
（前端與後端的串接、Godot 的 Backend.gd 不是我負責。）

---

## 一、CCND 概念網路圖（D3.js）

（截圖：`assets/ccnd-graph.png`）

- 情境：要把「一個人在對話中的推理脈絡」呈現出來。後端會算出一棵語意樹的資料，但資料本身不會變成圖。
- 做法：用 D3.js 的力導向圖把這棵樹畫成可縮放、有層次的網路圖，處理節點的視覺層次與縮放互動，並配合時間軸畫出某個時間點的樹。
- 結果：把抽象的推理結構變成看得懂的畫面。過程中練習了 D3 的資料綁定與力導向布局，也學到呈現層只處理整理好的資料、不碰資料怎麼算。

## 二、Godot 2D 虛擬大廳

（截圖：`assets/godot-lobby.png`、`assets/godot-issue.png`）

- 情境：一個 2D 多人世界，玩家相遇、張貼議題、用表情回應、聊天、語音。
- 做法：把跨端的網路呼叫（RPC）放在玩家節點、UI 只做本地畫面；議題張貼後廣播、頭頂泡泡即時更新；表情是「每位讀者一個、可覆蓋」；晚加入的人連上後會同步既有議題與表情；語音是本地音訊處理（變聲、殘響、頻譜），與網路分開。
- 結果：第一次做即時多人與音訊，學到多人同步靠節點路徑一致，以及把網路與畫面分層。

## 三、Godot 網頁顯示的除錯

（截圖：`assets/godot-letterbox-before-after.png`，可選）

- 情境：Godot 匯出成網頁嵌入後，畫面兩側出現黑邊。
- 做法：先判斷黑邊是引擎的 letterbox、不是前端 CSS 能處理的；把顯示設定的 stretch/aspect 從 keep 改成 expand 讓畫面填滿，再補一個底色處理填滿後露出的地圖外區域；最後發現「改了沒變」是瀏覽器快取加上 dev server 沒重開。
- 結果：一次除錯練習，學到先判斷問題在哪一層再逐一驗證，也記住產物被快取這種非程式的坑。
