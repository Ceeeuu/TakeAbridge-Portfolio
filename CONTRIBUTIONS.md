# My Contributions · Case Studies

我在 TakeABridge 負責**前端 UI/UX/視覺化**與 **Godot 2D 虛擬大廳**。以下用「**情境 → 我的做法/決策 → 結果**」整理幾個我實際做、也最能談的部分。

> 界線提醒：跟後端的 API/WebSocket 串接、以及 Godot 的 `Backend.gd`，**不是我負責**；下面聚焦在呈現、互動、視覺化與遊戲端。

---

## Case 1 — 對話結算畫面

> 🖼️ _[截圖位：`assets/settlement-envelope.png`（信封）、`assets/settlement-receipt.png`（收據結算頁）、可加 `assets/settlement-share.gif`（開封動畫）]_

### 情境
教授希望對話結束後有個「有樂趣」的結算畫面提升參與感。但這是**研究平台**——結算不能誤導受試者、更不能污染實驗數據。

### 我的做法 / 決策
- **產品+研究思維的取捨**：結算會給 S/A/B/C 評級，但我讓評級**只根據「思辨投入度」**（反思、對話品質、CCND 投入等量表），**刻意不看立場移動方向**。因為若「越去極化評級越高」，等於獎勵受試者為了分數故意改立場 → 污染去極化實驗。
- **一封信的儀式感**：後測送出 → 出現信封 → 點擊**開封動畫**（封蓋掀起）→ 滑出「收據 / 明細」風格的結算頁。
- **手刻 SVG 圖表**：立場軌跡（1–7 刻度尺，前→後 + 數字跳動）、思辨投入**雷達圖**、讚/倒讚**甜甜圈**。全部**手刻 inline SVG**，不裝任何圖表套件 —— 好處是配色/樣式完全可控、也方便匯出成圖。
- **無障礙配色**：圖表配色跑過色彩對比 / 色盲可辨識驗證（例如讚/倒讚不靠紅綠區分，改用綠 vs 暖灰 + 文字標籤）。
- **一鍵分享**：用 `html-to-image` 把多頁結算**疊成一張長圖 PNG** 讓使用者下載分享。

### 結果 / 學到
- 把抽象的實驗數值（`delta_s`、`stance_centrism`、投入量表）轉成使用者看得懂、想分享的畫面。
- 學到「視覺設計要服務研究目的」——好看之外還要**不破壞資料效度**，這個取捨是我最想談的一點。

---

## Case 2 — CCND 概念網路圖視覺化

> 🖼️ _[截圖位：`assets/ccnd-graph.png`（展開的力導向圖）、可加 `assets/ccnd-timeline.gif`（時間軸回放）]_

### 情境
系統要即時把「一個人在對話中的推理脈絡」呈現出來。後端會算出一棵「語意樹」（root → 錨點 → 觀點節點）的資料，但**資料不會自己變成圖**。

### 我的做法 / 決策
- 用 **D3.js 力導向圖（force-directed graph）**把階層/網路資料畫成可縮放、有層次、節點會自然散開的圖。
- 處理層次感（root/錨點/觀點不同視覺權重）、縮放與互動。
- 配合「時間軸回放」把某個時間點的樹快照畫出來（資料由上層提供）。

### 結果 / 學到
- 把最抽象的東西（推理結構）變成研究者和使用者都看得懂的視覺，是這個專案視覺化的核心價值。
- 學到 D3 的資料綁定與力導向布局，以及「呈現層只吃整理好的資料、不碰資料怎麼算」的分工。

---

## Case 3 — Godot 2D 虛擬大廳架構

> 🖼️ _[截圖位：`assets/godot-lobby.png`（世界全景）、`assets/godot-issue.png`（頭頂議題 + 表情列）]_

### 情境
要做一個 2D 多人社交世界：玩家相遇、在頭上張貼議題、用表情回應、發起聊天、語音互動。多人靠 Godot 自己的連線。

### 我的做法 / 決策
- **核心架構決定：RPC 掛在玩家節點、UI 只做本地呈現。** 每個玩家節點在每個 peer 上路徑一致（`/root/Game/<peer_id>`），這是跨端 `rpc_id` 能精準送達的關鍵；UI（螢幕空間 CanvasLayer）不含任何 RPC，只負責畫面，要做網路動作就呼叫本地玩家。→ 把**網路**與**呈現**分離。
- **議題 + 表情系統**：張貼議題廣播給所有人、頭頂泡泡即時更新；表情用「每位讀者一個、可覆蓋」的設計，作者端保存後廣播重畫。
- **晚加入同步**：新玩家一連上就請求同步，既有玩家把議題和表情補送給他（順序有講究）。
- **語音 DSP**：麥克風 → 變聲 / 殘響 / 頻譜 → 擷取傳送，本地音訊管線與網路解耦。（把變聲範圍收窄成 0.7–1.5，讓變聲後仍聽得懂。）

### 結果 / 學到
- 學到多人遊戲裡「狀態同步靠節點路徑一致」的思維，以及為什麼要把 RPC 和 UI 分層。
- 這是我第一次做即時多人 + 音訊處理，從架構分層到互動細節都自己踩過。

> 界線：Godot 對後端 Django 的呼叫（`Backend.gd`）不是我負責；上面講的是遊戲世界/互動/UI/音訊本身。

---

## Case 4 — Godot 網頁顯示 Debug（一個系統性除錯的故事）

> 🖼️ _[截圖位：可加 `assets/godot-letterbox-before-after.png`（黑邊 → 填滿 對比）]_

### 情境
Godot 匯出成網頁嵌進前端後，畫面兩側出現大片黑邊、遊戲被擠在中間。

### 我的做法 / 決策（重點在「怎麼找到根因」）
1. **先分清是誰的問題**：黑邊是引擎內部畫的 letterbox，不是前端 CSS 能蓋的 → 判定是 **Godot 顯示設定**。
2. **根因**：`stretch/aspect` 是預設的 `keep`（維持 16:9 → 比它寬就補黑邊）。改成 **`expand`** 讓畫面 edge-to-edge 填滿。
3. **expand 的副作用**：撐開後露出「地圖以外」的區域 → 把 `default_clear_color` 從黑改成灰，讓地圖外不突兀。
4. **最坑的一關**：改完卻「怎麼改都沒變」——最後發現是**瀏覽器快取 + dev server 沒重開**，一直跑舊的 build。硬重整 + 重啟後才正常。

### 結果 / 學到
- 一個很好的**系統性除錯**練習：先定位「問題在哪一層」（引擎 vs CSS vs 快取），再逐一驗證，而不是亂試。
- 學到 Godot web export 的 stretch/aspect、clear color、canvas 尺寸這些顯示機制，以及「產物被快取」這種非程式的坑。

---

## 我在這個專案的成長總結
- **視覺化能力**：把抽象資料（推理樹、立場數值）變成使用者看得懂、想分享的畫面。
- **分層思維**：前端「呈現 vs 串接」、Godot「網路 RPC vs UI 呈現」都刻意分開。
- **產品+研究的取捨意識**：好看之外還要不破壞實驗效度。
- **除錯方法**：先定位層級再驗證，遇到「改了沒效」先想快取/建置。
