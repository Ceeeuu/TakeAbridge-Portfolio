# 資料庫概要

主資料庫為 PostgreSQL 16，並啟用 pgvector 擴充，讓對話的語義向量（384 維）能與一般欄位存在同一張表；RAG 用的外部知識向量另存於 ChromaDB。資料庫設計與後端整合由**伍晨安**負責，各業務資料由對應模組的組員定義。

資料表依功能分為五個群組：

| 群組 | 大致存什麼 | 對應模組 / 負責 |
|---|---|---|
| 使用者與權限 | 沿用 Django 內建的帳號、群組與權限 | 伍晨安 |
| 立場量測與配對 | 每位使用者在議題下的問卷、立場分數、語義向量；配對佇列與配對結果、立場漂移紀錄 | 賴則名 |
| H-H 對話 | 對話房間、每則訊息（含情緒分數與語義向量）、AI 介入建議紀錄 | 賴則名 |
| H-AI 對話 | 每輪人機對話內容、對話 session 的語境與語意樹狀態 | 賴則名 |
| 觀點知識庫 | 篩選後沉澱的高品質觀點（含語義向量，供檢索）| 葉錦諦 |
| 系統輔助 | session、後台操作紀錄、資料庫遷移版本等 | 伍晨安 |

> 重點設計：對話語義向量與業務資料同存 PostgreSQL（pgvector），讓配對距離、立場漂移、觀點檢索都能直接用資料庫的向量運算。這部分由後端負責；我在前端是取這些資料來呈現（例如把語意資料畫成 CCND 圖）。
