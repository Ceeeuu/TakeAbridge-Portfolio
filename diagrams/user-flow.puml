@startuml user-flow
' 使用者操作流程 — 從大廳分兩條入口：一般對話 / Godot 虛擬大廳
skinparam backgroundColor #FFFFFF
skinparam shadowing false
skinparam defaultFontName "Noto Sans CJK TC"
skinparam ActivityBorderColor #8FA6BD
skinparam ActivityBackgroundColor #F3F6FA
skinparam ActivityFontColor #2C3E50
skinparam ActivityDiamondBackgroundColor #FBF3E0
skinparam ActivityDiamondBorderColor #C9A96A
skinparam ArrowColor #5A6B7B
skinparam NoteBackgroundColor #FFFDF5
skinparam NoteBorderColor #C9A96A

start
:登入平台\n(JWT 身分驗證);
:進入系統大廳\n選擇對話議題，或進入虛擬大廳;

split
  -> 一般對話入口;
  :填寫立場問卷\n8 題李克特量表 + 2 題開放式;
  :系統計算立場分數\n生成 384 維語義向量;

  if (立場分類) then (中立池)
    :生成對立立場 AI 代理人\n(RAG 事實注入);
    :進入 H-AI 對話室;
  else (支持池 / 反對池)
    :異質配對演算法\n0.6 × 李克特距離 + 0.4 × 語義距離;
    :配對成功，跳轉對話室;
    :進入 H-H 真人對話室;
  endif

  :即時對話\n(左：訊息介面　右：CCND 力導向圖);
  note right
    對話全程守護：
    · 攻擊性內容即時攔截
    · 情緒過高推送冷靜提示
    · 離題引導回主題
    · 僵局提示 · 立場漂移追蹤
  end note

  :主動結束或沉默超時;
  :產出量化分析報告\n立場偏移量 · 情緒變化趨勢;
  :填寫後測問卷\n6 題李克特 + 2 題開放式;

  if (通過品質篩選?) then (是)
    :沉澱為觀點知識庫\n(pgvector 語義檢索);
  else (否)
  endif
  stop

split again
  -> Godot 虛擬大廳入口;
  :進入 Godot 虛擬大廳\n選基礎像素角色、隨機分配 ID;
  :操控像素青蛙分身\n在議題廣場自由漫遊;
  :發表自己的議題\n(頭頂泡泡呈現給周圍玩家);
  :走近他人、讀其議題\n用五種表情標籤快速表態;
  stop
end split

@enduml
